class Space
  # To be overriden by child classes.
  # player is usually the player landing on the space
  # Bank is the single instance of the (yet nonexistent) Bank class
  # players is an array of all players in the game for spaces
  # that affect multiple players
  def play(player, bank = nil, players = nil)

  end
end